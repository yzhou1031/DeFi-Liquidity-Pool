# Claude Code — Project Guidelines

## File Format

**All Python code in this project must be in `.ipynb` (Jupyter notebook) format — not `.py` files.**

When writing or modifying Python code, always create or edit `.ipynb` files. Never create standalone `.py` scripts.
